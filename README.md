# PoeBuilder 0.9.1 — вкладка самоцветов, честные резисты, тултипы

> **0.9.1:** error.log больше не разбухает — повторяющиеся исключения (как 33 тысячи в твоём старом логе) считаются и пишутся одной строкой-счётчиком. Проверено: лог от старой сборки больше не воспроизведётся такой лавиной.
>
> **0.9.0 — по твоим шести замечаниям:**
> 1. **Тултипы починены везде**: белый прямоугольник был нестилизованной подсказкой WPF — теперь все подсказки тёмные, в стиле игры (умения, предметы, слоты, самоцветы). У предметов при наведении — полный текст (уники — как в игре, остальные — аффиксы с подставленными числами), плюс честная строка про качество: «на расчёт пока не влияет».
> 2. **Уникальные предметы создаются**: в редакторе предмета — поиск по закреплённому списку из 449 уников; выбор подставляет имя и редкость «Уникальный». Честно: модификаторов уников в закреплённом каталоге нет (RePoE их не экспортирует), поэтому их текст вписывается руками из игры — в расчёт уники пока не влияют.
> 3. **Новая вкладка «Самоцветы»**: список самоцветов билда, конструктор (редкость, аффиксы из пула самоцветов с числами, качество) и **посадка в гнёзда дерева**: аллоцируй узел-гнездо на «Дереве» (1 очко) → гнездо появится во вкладке → «Вставить». Аффиксы посаженных самоцветов действуют глобально (кроме «в радиусе» — они честно помечены как не влияющие, модель радиуса ещё не сделана).
> 4. **Починен переключатель Стартер/Эндгейм**: привязка радио указывала на несуществующее свойство — стадия вообще не менялась, поэтому и было всегда 0%. Теперь на «Эндгейме» огонь/холод/молния = **−40%**, хаос 0%, а вклад дерева и шмота — рядом в строке («от дерева и снаряжения: +N%»), не суммируется.
> 5. **Лист персонажа теперь пересчитывается сразу** при любом изменении (снаряжение/умения/дерево/стадия) — раньше он реагировал только на уровень и имя, поэтому цифры «не менялись» после импорта. Импортируй свой PoB-код заново: снаряжение переносится (у твоего: 13 предметов + 5 самоцветов + 10 уников), и цифры урона растут (Flameblast 3206/с на твоих данных). Полного совпадения с игрой пока нет: «Gain N% … as Extra …» и часть строк не считаются — это видно в отчёте импорта.
> 6. **Мегаломания и сокеты из PoB переносятся точно**: самоцветы встают в те же гнёзда дерева (7960, 21984, 26196, 55190, 61419 у твоего билда), а её строки «Allocates …» аллоцируют ноты **бесплатно и без пути** — как в игре: лишние точки на путь больше не тратятся. Если после импорта дерево всё ещё выглядит с лишним путём к старту друида — скажи: значит, механику «старт с ветки другого класса» несёт другой предмет, и мне нужно его точное название из игры.
>
> **0.8.1:** исправлено падение при запуске (XamlParseException из подсказки списка предметов — триггер стиля внутри шаблона заменён на конвертер).
>
> **0.8.0 — по твоим восьми замечаниям:**
> 1. **Конверсия урона** (Стрела молнии: «80% физического урона становится уроном молнии») — расчёт теперь читает статические данные камней: сначала `%_to_convert_to_` (перенос), затем `%_to_add_as_` (дополнение), порядок типов: физ→огонь→холод→молния→хаос. Проверено тестом: лук + Стрела молнии = ровно 80% молнии в разбивке.
> 2. **Крит до сотых** — шанс крита считается как база × (1 + увеличение с дерева/снаряжения) × «больше», отображается с двумя знаками (6,11%), бонус крита умножается на «больше»-множители.
> 3–4. **Самоцветы и снаряжение из PoB-кода** — импорт переносит предметы: слоты PoB («Weapon 1», «Helmet», «Ring 1», «Charm N»…) сопоставляются с нашими 17 слотами, строки аффиксов сверяются с закреплённым каталогом модов, редкие предметы получают свои значения. **Самоцветы** (включая уник-самоцветы вроде «Мегаломания») импортируются как предметы инвентаря — гнёзва дерева и их перки пока не моделируются (это указано в отчёте импорта). «Weapon 1 Swap» трактуется как левая рука набора 1 — если в PoB2 это не так, скажи, поправлю.
> 5. **Снаряжение влияет на расчёт** — броня, ЭС, уклонение, локальные моды оружия, плоские добавки урона, сопротивления, атрибуты, скорость атаки/каста из надетых предметов входят в итоговый лист. На реальном билде из твоего PoB-кода: импортировано 13 предметов в слоты + 5 самоцветов + 10 уников; Flameblast вырос с 866 до 3206 урона/с.
> 6. **Ховер умения = описание из игры** — описание камня + построчные значения текущего уровня из закреплённого источника + строка «Качество: N%». Описания пока на английском (проверенных официальных RU-текстов нет). Качество — как метаданные: в расчёт качество камней пока не входит, это указано.
> 7. **Удаление умений** — у каждой карточки умения появилась кнопка ✕ (удаляет всю группу вместе с поддержками, с подтверждением).
> 8. **Сопротивления — только базовая линия этапа**: «Стартер» = 0%, «Эндгейм» = −40% огню/холоду/молнии (хаос не штрафуется). Вклад дерева и снаряжения больше не подмешивается в число — он показан рядом с каждой строкой («от дерева и снаряжения: +N%»).
>
> Честные ограничения 0.8.0: уникальные модификаторы в закреплённом каталоге отсутствуют (RePoE экспортирует только идентичность уников) — уники переносятся со своим полным текстом в подсказке и **не влияют на расчёт**; часть строк аффиксов может не сопоставиться с каталогом — они считаются в отчёте импорта и не влияют на расчёт; моды «Gain N% … as Extra …» из снаряжения в расчёт пока не входят.

> **0.7.1:** импортёр проверен на **реальных примерах из твоего сообщения** — они добавлены в тесты как постоянные фикстуры. Код Path of Building оказался экспортом **PoB2** (корень `PathOfBuilding2`, `treeVersion 0_5` = наше закреплённое дерево 0.5.5): парсер теперь понимает PoB1 и PoB2, атрибуты `ascendClassName`/`ascendancyInternalId` («Mercenary3»), умения в `<Skill><Gem nameSpec= skillId=>`, каскадный поиск камней по `skillId` и названию, реальные уровни камней из кода, лишние активные камни (Arc в группе Spell Totem) раскладываются в поддержки — как в официальном экспорте игры. Узлы дерева и восхождения аллоцируются связным путём: восхождения PoB перечисляет без соединяющих узлов — путь достраивается автоматически (в отчёте: «+N соединяющих узлов»). Узлы-выборы восхождений (multiple choice, например «Implanted Gems») планировщик пока не поддерживает ни вручную, ни через импорт — они честно перечислены в отчёте. Итог на твоих примерах: **PoB2-код: 136/136 узлов пути, 17/17 умений, 38/38 поддержек, 0 нераспознанных камней; JSON poe.ninja: 130/133 узлов, 14/14 умений, 38/38 поддержек** — остальные в отчёте с причинами.

 — импорт/экспорт и лист персонажа

> **0.7.0:** по твоим шести замечаниям. **Импорт билдов**: код обмена Path of Building (декодируется локально: base64url → zlib → XML), JSON официального формата Build Planner v1 (файл `.build`, экспорт poe.ninja — это один и тот же формат), вставка JSON текстом или скачивание по ссылке — один раз, только по нажатию «Импортировать». **Экспорт** в тот же официальный формат `.build` — файл кладётся в `Documents\My Games\Path of Exile 2\BuildPlanner` и открывается игрой. По итогу импорта — честный отчёт: сколько пассивов/умений найдено, какие идентификаторы не распознаны. **Вкладка «Конфигурация» убрана**: те же поля (название, класс, восхождение, уровень, стадия, версия) правятся кнопкой «Изменить» на карточке билда. **«Умения»**: быстрое добавление = само умение + уровень + качество + «Добавить» (группа называется по умению), при наведении на значок умения — игровое описание камня. **«Персонаж» — итоговый лист только для чтения, поделён пополам**: слева умения с поддержками; стрелочка раскрывает цифры (средний удар, удары/с, крит, мана, разбивка физ/огонь/холод/молния/хаос), справа защита, атрибуты, скорость и **широкая таблица сопротивлений**. Починены радио «Стартер/Эндгейм» (конвертер инверсии возвращал само значение — обе галочки ставились сразу).
>
> Язык тултипов камней: описания берутся из закреплённого каталога RePoE и **пока на английском** — полных официальных русских текстов в проверенных источниках нет; как только появятся проверенные RU-данные, они будут подставлены без изменения формата. Формат `.build` у GGG помечен «Version 1 (Experimental)» — возможны изменения формата со стороны игры. Импорт переносит пассивы (включая восхождения), умения и поддержки; предметы чужих форматов не переносятся — это указано в отчёте импорта. Код PoB несёт целочисленные id узлов — они напрямую совпадают с id нашего закреплённого дерева; уровни активных камней из PoB намеренно берутся 1-ми (в отчёте сказано проверить вручную), уровни поддержек — как в коде.

> **0.6.0:**

> **0.6.0:** по твоим девяти замечаниям. Каталог v3: **пулы модов объединены по классам предметов** (лук больше не отклоняет свои моды) + **осквернённые моды из данных RePoE**. В редакторе предмета: чекбокс «Осквернён» и выбор осквернённого мода, удаление любого добавленного мода/вставки кнопкой ×. Экран «Персонаж»: выбор стадии **«Стартер» (сопр. 0%) / «Эндгейм» (−40% стихиям)** в Конфигурации. Создание билда: **класс и восхождение из списков** (старт дерева следует за классом), **версия из перечня реальных патчей** (0.1.0…0.5.5c), без ручного ввода. **«+N к уровню всех умений снарядов» на луке теперь работает**: уровень камня растёт, урон пересчитывается, на карточке и в таблице видно «+N ур. от предметов». Инвентарь выровнен, в пустых слотах — мини-иконки всех типов. Больше статов урона из дерева учтено (луки/арбалеты/мечи/снаряды/ближний бой/области и комбинации с типом урона); неучтённые по-прежнему показываются честно.

> **0.5.2 (hotfix):** найдена причина краша 0.5.0/0.5.1 при создании/открытии билда — привязка `<Run Text="{Binding}"/>` в списке «Допущения» на странице «Персонаж» не имела пути, а `Run.Text` в WPF двусторонняя по умолчанию → `XamlParseException «Для двусторонней сборки требуется Path или XPath»` при первом отображении страницы. Привязка заменена на обычные TextBlock-и, всем `Run` задан явный `Mode=OneWay`. Повторяющиеся одинаковые ошибки больше не заваливают диалогами: повтор в течение 5 секунд пишется только в журнал `error.log`.
>
> **0.5.1:** исправлены клики по сокетам поддержек; журнал сбоев `%LOCALAPPDATA%\PoeBuilder\Native\error.log`; изоляция привязки модулей при создании/открытии билда.


**C# · WPF · .NET 10 · Visual Studio 2026**. Самостоятельный проект, без исходников/рантайма PoB, Lua или SimpleGraphic. Во время работы не нужны сеть, Steam или файлы установленной игры.

> Не полный аналог PoB: реализованы библиотека, заметки, интерфейс RU/EN, дерево и восхождения, снаряжение и группы умений с иконками, а также **первые честные расчёты (v1)** на отдельном экране «Персонаж». Поддержки пока не изменяют урон; конверсии, бафы и враги не моделируются. Полная русская локализация игровых строк не готова.

## Что нового в 0.5.0

1. **Игровые иконки предметов.** Надетое снаряжение в инвентарном стиле: слоты с иконками из арт-ресурсов игры, цветной рамкой редкости, призраками классов в пустых слотах. 1355 PNG, источники и контрольные суммы — в `Data/Icons/manifest.json` + `NOTICE.txt`. Арт © GGG из публичных зеркал; соответствие патчу не зафиксировано.
2. **Экран «Персонаж» — расчёты v1.** Ресурсы (жизнь, мана, ЭС с восстановлением, дух), защита (броня с честной оценкой снижения, уклонение, точность, блок, отклонение, скорость передвижения, реген), атрибуты и сопротивления с капом 75% (+максимумы) — как на экране «C» в игре, по числам. Всё пересчитывается при изменении дерева, снаряжения, умений и уровня.
3. **Урон каждого умения.** Таблица по группам: DPS, средний удар, удары/сек, крит (шанс/бонус), стоимость маны, состав урона по типам. Атаки считаются от оружия (время атаки, диапазон, крит оружия + локальные моды), заклинания — от поуровневых значений камня. Базовый бонус крит. урона 100% (крит ×2).
4. **Умения с иконками и 5 сокетами.** Карточка группы: круглый камень + пять круглых сокетов. Пустой сокет открывает выбор поддержки с иконками и поиском (уже вставленные скрыты), крестик снимает поддержку. DPS группы виден прямо на карточке.
5. **Честность расчётов.** На экране «Персонаж» — блоки «Не учтено» (какие статы не вошли) и «Допущения и источники» (откуда каждая формула). Поддержки, качество предметов, конверсии урона и условные эффекты в v1 **не** применяются — это видно в интерфейсе, а не спрятано.
6. **Тесты расчётов.** +10 исполняемых проверок: формулы уровней/атрибутов, Fireball (8.9 ур/с), атака от оружия (17.9 ур/с), локальные моды, кап сопротивлений, заметки честности. Всего **87 тестов**.

Каталог данных прежний (RePoE 4.5.5.2, EN; дерево GGG 0.5.5). Соответствие патчу **0.5.5c не подтверждено** — указано в интерфейсе.

## Запуск и безопасное обновление

1. Закрой работающий PoeBuilder. Если есть важные билды, скопируй `%LOCALAPPDATA%\PoeBuilder\Native\` в резервную папку.
2. Распакуй 0.5 **отдельно от работающего проекта**, например `C:\Users\supamodd\source\repos\PoeBuilder-0.5-check`.
3. Открой `PoeBuilder.sln`, стартовый проект — **PoeBuilder.App**, **Debug | Any CPU**, **F5**. Нужны Visual Studio 2026, нагрузка **.NET desktop development** и .NET 10 SDK.
4. Создай/открой билд → «Предметы» → надень оружие и доспех → «Умения» → группа с активным умением и поддержками → «Персонаж» — там все метрики и урон умений.

`Data\Tree\`, `Data\Game\` и `Data\Icons\` копируются к приложению автоматически (суммарно ~30 МБ ресурсов). Prepare.cmd, распаковка клиента игры и установка Python не нужны. На первой сборке SDK может восстанавливать стандартные компоненты .NET.

### Формат билдов

Формат не менялся: **schema 4** (дерево + снаряжение + умения). Файлы 0.4 совместимы с 0.5 без миграции. При явном сохранении создаётся предыдущая версия `.bak`. **Приложения 0.3 и старше не читают schema 4.**

Папка данных прежняя:

```text
%LOCALAPPDATA%\PoeBuilder\Native\
  settings.json
  Builds\<id>.poebuild
  Builds\<id>.poebuild.bak
  Builds\Trash\
```

Импорт создаёт новый ID; экспорт не заменяет внутренний файл. Чужой набор данных сохраняется как исходный план, но его редактирование блокируется. Очистка планов — только явная, с подтверждением. У каждого модуля (дерево, снаряжение, умения) своя история undo/redo.

Для восстановления `.bak` скопируй его вне каталога данных, переименуй копию в `.poebuild`, импортируй. Билды из корзины также можно импортировать.

## Управление

Дерево:

- Щелчок: свойства и предпросмотр маршрута.
- Двойной щелчок / кнопка / Enter: выделить маршрут.
- Delete / кнопка: снять узел и отключившиеся ветки; каскад требует подтверждения.
- Колесо: масштаб относительно курсора; перетаскивание левой/средней кнопкой: перемещение.
- «Всё дерево»: вместить текущий граф; «К старту класса»: к старту текущего графа.
- Кнопки ↶/↷: общая история обоих деревьев, до 100 шагов. Ctrl+Z/Ctrl+Y работают **при фокусе на поле**, не перехватывая Undo заметок.
- Ctrl+S: сохранить; Ctrl+N: новый; Ctrl+O: импорт собственного `.poebuild`.

Без открытого билда доступен просмотр основного дерева. Свободная подпись класса в «Конфигурации» — метаданные, не выбор игрового класса.

Снаряжение:

- Слот с иконкой → редактор предмета: база (с иконками и поиском) → модификаторы из пула базы → качество → гнёзда с рунами/ядрами/идолами → сохранить/надеть. Тултип в стиле игры обновляется на лету.
- Список хранилища → изменить/надеть/снять/удалить/дублировать. Переключение наборов оружия меняет видимую пару слотов; второй набор остаётся надетым.
- Несохранённый черновик редактора защищён запросом при закрытии окна.

Умения:

- Карточка группы: иконка камня, уровень/качество, набор, DPS и 5 сокетов. Сокет → выбор поддержки (иконки + поиск), крестик → снять.
- Редактор группы (карандаш): полное редактирование как в 0.4. Отключённая группа хранится, но помечена крестом.

## Границы расчётов v1 (важно)

- Поддержки **не** изменяют DPS (их множители — следующий этап); на карточке и в таблице есть пометка.
- Качество предметов и камней не влияет на числа; конверсии урона внутри умений не применяются («получить как» для атак — применяется).
- Бафы, заряды, айменты, вампиризм и configurable enemy damage не моделируются. Player и monster hit chance показываются только как rating-vs-rating estimate против стандартного монстра того же уровня; attack block теперь cap-ится по текущему PoB2 reference, deflection показывает rating и chance против accuracy default monster. Spell block и dodge не подключены; suppression cap/effect helper существует, но player source отсутствует в текущем catalog и suppression пока не входит в EHP. Enemy config, entropy и полный combat pipeline не подключены. ES recharge rate modifiers и базовый 4-секундный start delay показываются, но interruption state отсутствует. Броня — оценка против монстра уровня персонажа. Защитный EHP показывается как typed-вектор для одного успешного hit-сценария (physical/fire/cold/lightning/chaos): raw hit берётся из `PhysicalDamage` того же default monster, physical/elemental pool = Life + Energy Shield, chaos pool = Life + половина Energy Shield из-за двойного урона хаосом по ES, armour/resistance используют текущие estimates. Hit chance, block, deflection, suppression, recovery, penetration/exposure, explicit chaos bypass и прочая enemy configuration в EHP не включены; это не универсальный scalar EHP и требует target-patch verification.
- Внутренние сопротивления: effective value = baseline стадии + сырые источники дерева/снаряжения/самоцветов; верхний кап 75% + maximum resistance. Starter baseline = 0%, Endgame baseline = −40% для огня/холода/молнии, chaos не штрафуется. Нижний предел и полная enemy/penetration модель требуют отдельной верификации.
- Уровень: +12 жизни / +4 маны / +6 точности / +3 уклонения за уровень; сила +2 жизни, ловкость +5 точности, интеллект +2 маны (poe2.dev 0.5.x, maxroll 18.09.2026). Каждый расчёт сопровождается источниками прямо в UI.
- Дерево: источник — фиксированный официальный экспорт **GGG 0.5.5**, commit `bd87e6512c92b868542eddfb1ba4ea8b6dc2da36`. Соответствие клиенту **0.5.5c не подтверждено**.
- Основной граф не содержит мостов восхождений. Каждый мини-граф изолирован. Пулы очков раздельные и **ручные** (восхождение — 8 по умолчанию). Безусловные `isFree` узлы стоят 0 очков и включаются по выбранному пути.
- Варианты внутри узлов (`isMultipleChoice`), условные/специальные выделения, grant-passive-points и weapon-set перераспределение **пока заблокированы**.
- Гнездо дерева можно выделить и затем заполнить на вкладке «Самоцветы». Глобальные аффиксы самоцветов учитываются; эффекты «в радиусе» и специальные unique mechanics пока ограничены.
- Artwork уникальных предметов и самоцветов загружается по закреплённому `Art/...` пути с `web.poecdn.com` при необходимости; приложение не падает без сети, но картинка может быть недоступна.
- Снаряжение: базы, обычные/волшебные/редкие предметы, уникальные identity и импорт поддерживаемых PoB-строк подключены. Полная модель unique modifiers, крафт и все socketable family rules ещё не реализованы. Число гнёзд задаётся вручную.
- Умения: максимум 5 поддержек на группу; совместимость поддержек с активным умением не проверяется (в источнике только рекомендации).

## Локализация

Переключатель RU/EN переводит собственный UI, но **не игровой контент**: имена предметов и камней остаются EN (RePoE). Полного проверенного RU-набора нет; автоперевод не применяется. Текущий английский fallback — известное незавершённое требование.

## Проверки

```powershell
dotnet build PoeBuilder.sln -c Debug
dotnet build PoeBuilder.sln -c Release
dotnet run --project tests\PoeBuilder.Tests -c Release
```

**87 тестов**: прежние 77 (дерево, восхождения, каталог, правила, миграции) + 10 расчётных (формулы, Fireball, атака, локальные моды, капы, честность). Это исполняемый тестовый проект, не Test Explorer/xUnit.

Здесь проверяются сборка C#/XAML и логика в Linux с SDK 10.0.401. **WPF 0.5 на Windows здесь не запускался.** Новая визуальная проверка обязательна. См. `docs/VALIDATION.md` и `docs/WINDOWS-CHECKLIST.md`.

## Репозиторий

Обновление подготовлено поверх 0.4. В `supamodd/PoeBuilder` ничего не отправлялось. После F5-проверки создай ветку, например `feature/character-calculations`, сравни и перенеси исходники из архива. Не удаляй `.git`; не публикуй `bin/obj`, `.vs`, личные билды или настройки. Если менял проект после 0.4, объединяй изменения, а не заменяй файлы вслепую.

## Структура и лицензии

- `Core/Tree`: каталог дерева, планы, изолированные графы, weighted shortest-path, refund, история.
- `Core/Equipment`, `Core/Skills`: каталог RePoE, планы снаряжения и умений, правила слотов/модов/вставок/групп.
- `Core/Calculation`: `GameStatMap` (закреплённые строки → статы), `StatInterpreter`, `CharacterCalculator` v1.
- `App/ViewModels`: `TreeViewModel`, `EquipmentViewModel` + `ItemDraftViewModel`, `SkillsViewModel` + `GroupDraftViewModel`, `CharacterViewModel`.
- `App/Services`: `IconService` (иконки из bundled-арта), `CalculationHub`, `ViewportSizing`, конвертеры.
- `App/Controls/TreeViewport`: immediate-mode WPF, камера каждого графа, иконки и портреты.
- `Data/Tree/manifest.json`, `Data/Game/manifest.json`, `Data/Icons/manifest.json`: источники, контрольные суммы и преобразования ресурсов.

Собственный код — MIT. **Игровые данные, тексты и изображения GGG не MIT**; см. `THIRD-PARTY-NOTICES.md` и `Data/Icons/NOTICE.txt`. Иконки предметов/камней — арт Path of Exile 2 © GGG из публичных зеркал; распространение вместе с приложением требует твоего решения (см. вопрос в чате). Отдельный декоративный фон библиотеки создан генеративным инструментом, не является артом игры.

**This product isn't affiliated with or endorsed by Grinding Gear Games in any way.**
